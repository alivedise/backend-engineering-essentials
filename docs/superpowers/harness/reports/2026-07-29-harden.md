# Harden Run — 2026-07-29

First BEE harden run, batch size 2, on the harness ported from FEE the same
day. The run needed one mid-flight harness fix: the verify stage initially
condemned both articles for EN/zh parity drift, which the pipeline's own Sync
stage resolves later. The verify prompt now excludes cross-file zh parity;
the run was resumed from cache with only verify rounds re-executed.

## Revised

- BEE-9003 docs/en/Caching/cache-eviction-policies.md — findings {tone: 4, organization: 2, references: 3, factual: 5, template: 2}.
  Template restructured to the canonical skeleton (Context/Visual/Example/Best
  Practices/Related Topics/References plus four topic-specific sections). LFU
  worked example corrected (evicts B, not D, per the article's own recency
  tiebreaker). A fabricated Redis "recommendation" blockquote replaced with
  prose verified against the live Redis eviction docs; Redis 8.6 LRM policies
  added to the policy table. Dan Luu citation corrected (plain random and FIFO
  are strictly worse than LRU; 2-random is the competitive variant). S3-FIFO
  (SOSP 2023) and SIEVE (NSDI 2024) sections added with all numbers verified
  against the papers; Ristretto claim corrected (TinyLFU admission + sampled
  LFU eviction, not full W-TinyLFU). zh-TW synced (27 passages) plus zh-only
  fixes: removed 「不是X，而是Y」 frame, dash appositives, and a
  「其核心概念是：」 preamble; mirrored the LFU fix and new sections.
  Minor residuals for the reviewer:
  - SIEVE section omits the paper's own scan-resistance caveat (S7.2: SIEVE is
    not scan-resistant on block workloads with scans), in an article that uses
    scan resistance as a selection criterion.
  - "Both consistently outperform plain LRU and LFU on diverse workloads"
    overstates the Ristretto half (its README claims parity with exact LRU on
    some traces, better on Search/Database traces).

## Clean

(none)

## Reverted / Failed (needs manual attention)

- BEE-9002 docs/en/Caching/cache-invalidation-strategies.md — findings {tone: 4, organization: 5, references: 5, factual: 8, template: 3}.
  The audit found real problems (broken BEE-9002 self-links in Related Topics,
  pre-template section structure, 8 factual issues including a wrong stale-set
  race interleaving) and the revision fixed many, but each verify round
  surfaced new blocking findings in the revised text: Varnish grace-mode
  semantics ("grace mode marks nothing stale"), an impossible MGET claim for
  versioned keys (MGET needs all key names up front, but the data key name
  depends on the version), and an internal contradiction between the Soft
  Invalidation section and the stampede-prevention bullet. Reverted after
  three rounds per policy. Needs a manual harden pass; the audit findings in
  this run's journal are a usable worklist.

## zh desync (needs manual attention)

(none)
